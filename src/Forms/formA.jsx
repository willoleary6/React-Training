import React from 'react';
import Button from '../components/Button.jsx'

const formA = (props) => {
    return(
        <div>
            <Button
                title = {"Hello"} />
            <Button
                title = {"World"} />
        </div>

    )
}
export default formA;